<!DOCTYPE html>
<html>
<head>
<title><%= t('classifio') %></title>
<%= stylesheet_link_tag "application", :media => "all" %>
<%= javascript_include_tag "application" %>
<%= javascript_tag "$('document').ready(function() { Classifio.init() });" %>
<%= csrf_meta_tags %>
</head>
<body>
<div id="header" class="header">
</div>
<div class="container">
  <div class="hero-unit">
    <h1><%= t('classifio') %></h1>
    <p><%= t('tagline') %></p>
  </div>
  <%= yield %>
</div>
</body>
</html>
